import './styles.css'

const root = document.getElementById('root')

if (root) {
  root.innerHTML = `
    <main class="newtab">
      <h1 class="newtab_title">Browser Launchpad</h1>
      <p class="newtab_subtitle">Your new tab is now overridden. Features coming soon.</p>
    </main>
  `
}
